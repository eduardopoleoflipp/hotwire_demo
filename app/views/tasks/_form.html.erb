<%= form_with(model: task, html: { class: "ui form" }) do |form| %>
  <%= form.hidden_field :project_id, value: project&.id, id: 'form-project-id-input' %>

  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="two fields">
    <div class="field">
      <div class="ui input">
        <%= form.text_field :name, placeholder: 'Enter Task' %>
      </div>
    </div>
    <div class="field">
      <div class="actions">
        <%= form.submit "Create", class: "save-button"%>
      </div>
    </div>
  </div>
<% end %>
